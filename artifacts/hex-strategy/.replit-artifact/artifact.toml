kind = "mobile"
previewPath = "/"
title = "Hex Battles"
version = "1.0.0"
id = "artifacts/hex-strategy"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 24998

[services.development]
run = "pnpm --filter @workspace/hex-strategy run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/hex-strategy", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/hex-strategy", "run", "serve" ]

[services.env]
PORT = "24998"
BASE_PATH = "/"
